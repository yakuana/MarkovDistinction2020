import React from 'react';

function Header() {
    return (
        <div id="header-container">
            <h1 id="header">Thumbs up or Thumbs Down?</h1>
            <p id="header-desc">Predicting image selection patterns based on 10 seconds of user interaction.</p>
        </div>
    );
}

export default Header;